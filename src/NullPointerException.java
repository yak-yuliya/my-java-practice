public class NullPointerException {
    // null reference - a reference to nothing
    //this happens when you create an object, which creates a variable name,
    //and an address where the data will reside, but it is not then instantiated
    //keyword for instantiation is NEW
}
